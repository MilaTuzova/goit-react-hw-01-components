// import { formatEventDuration } from 'date-fns';

// export const formatEventDuration = (start, end) => {
//     return formatDistanceStrict(Date.parse(start), Date.parse(end));
// }


// --- import { formatEventDuration } from 'utils/formatEventDuration';